//identify a prime
//(math.spart(n)+1) is in float type; can not be used as a parameter in range function


import math
def prime(n):
    P=True;
    for i in range (2,int(math.sqrt(n)+1)):
        if n%i==0:
            P=False
            break
    if P:
        print('是')
    else:
        print('否')
        

n=int(input())
prime(n)
